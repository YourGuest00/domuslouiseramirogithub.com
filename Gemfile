# Gemfile for local Jekyll + Writer theme development
source "https://rubygems.org"

gem "jekyll", "~> 4.3"
gem "jekyll-feed"
gem "jekyll-remote-theme"

# Optional — use GitHub Pages environment
# Uncomment the next line if you want to match the exact environment GitHub uses:
# gem "github-pages", group: :jekyll_plugins
